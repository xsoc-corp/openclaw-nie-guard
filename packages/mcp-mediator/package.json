{
  "name": "@xsoc/mcp-mediator",
  "version": "0.1.0",
  "private": true,
  "description": "MCP boundary mediation. Validates and sanitizes MCP requests and responses flowing through OpenClaw. Tags external content for agent system-prompt awareness.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@xsoc/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
