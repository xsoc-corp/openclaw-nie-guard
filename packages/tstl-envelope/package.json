{
  "name": "@xsoc/tstl-envelope",
  "version": "0.1.0",
  "private": true,
  "description": "TSTL continuity envelope creation and validation. Application-level schema; cryptographic sealing is delegated to NIE bindings.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@xsoc/shared-types": "workspace:*",
    "@xsoc/nie-bindings": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
